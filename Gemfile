source 'http://rubygems.org'

gem 'rails', '3.0.0'
gem 'sqlite3-ruby', :require => 'sqlite3'
gem 'rpx_now'

group :development, :test do
   gem 'capybara'   # For integration testing, replaces webrat
   gem 'database_cleaner' # For cleaning databsase between tests
   gem 'cucumber-rails'
   gem 'cucumber'
   gem 'rspec-rails', '>=2.0.0.beta.5'
   gem 'rspec', '>=2.0.0.beta.5'
   gem 'spork' # Server for running tests in a clean state
   gem 'launchy'    # So you can do Then show me the page
end

